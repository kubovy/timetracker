<%= form_for(@holiday) do |f| %>
  <%= f.hidden_field :employer_id, :value => selected_employer_id %>
  <% if @holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% @holiday.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>One date per line in DB format: YYYY-MM-DD</p>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.text_area :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
